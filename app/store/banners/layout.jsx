import { pageMetadata } from "@/lib/metadata";

export const metadata = {
  title: "Banner Management | VM Store Seller",
  description: "Upload and manage promotional banners for your store"
};

export default function BannerLayout({ children }) {
  return children;
}
